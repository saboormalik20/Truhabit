extends baseEmail

block content
    // CONTENT
    p(style="color:black;font-size:20px; font-weight:bold") Username:-
    p(style="color:black") #{username},
    p(style="color:black;font-size:20px; font-weight:bold") Email:-
    p(style="color:black") #{tos}
    p(style="color:black;font-size:20px; font-weight:bold") Message:-
    p(style="color:black") #{message}
    //- p(style="color:black") We're all a big familiy here, so make sure to upload your user photo so we get to know you a bit better!
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td

     
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td